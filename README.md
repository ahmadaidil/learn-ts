# learn-ts

Install this as global before run
```command
$ yarn global add typescript ts-node
```

How to run
```command
$ ts-node <filename.ts>
```

How to compile (*.js)
```command
$ tsc <filename.ts>
```
